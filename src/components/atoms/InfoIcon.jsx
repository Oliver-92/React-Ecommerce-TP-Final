const InfoIcon = ({ name }) => {
// InfoIcon: Componente atómico para mostrar íconos informativos usando Bootstrap Icons.
  return <i className={`bi bi-${name} text-5xl`}></i>;
};

export default InfoIcon;
